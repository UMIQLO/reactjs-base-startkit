# ReactJs Base StartKit
### Installation
ReactJs Base StartKit requires [yarn](https://yarnpkg.com/) to run.

Install the dependencies and devDependencies and start the server.

```sh
$ cd reactjs-base-startkit
$ yarn
$ yarn start
```